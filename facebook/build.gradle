apply plugin: 'android-library'

dependencies {
    compile 'com.android.support:support-v4:19.1.+'
    compile files('./libs/bolts.jar')
}

android {
    compileSdkVersion 19//Integer.parseInt(project.ANDROID_BUILD_SDK_VERSION)
    buildToolsVersion '20.0.0'//project.ANDROID_BUILD_TOOLS_VERSION

    defaultConfig {
        minSdkVersion 10//Integer.parseInt(project.ANDROID_BUILD_MIN_SDK_VERSION)
        targetSdkVersion 19//Integer.parseInt(project.ANDROID_BUILD_TARGET_SDK_VERSION)
    }

    lintOptions {
        abortOnError false
    }

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            res.srcDirs = ['res']
        }
    }
}
